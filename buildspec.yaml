version: 0.2

phases:
 install:
  runtime-version:
     nodejs: 14
    commands:
     -npm install
     build:
      commands:
        -npm run build
artifacts:
files:
 - '**/*'
 base-directory: build
